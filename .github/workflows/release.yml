name: Release

on:
  push:
    tags:
      - "v*"

jobs:
  release:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v6

      - name: Setup .NET
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: "10.0.x"

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y clang zlib1g-dev libalpm-dev libgtk-4-dev zip gettext valac meson ninja-build libjson-glib-dev

      - name: Publish Shelly.Cli
        run: |
          dotnet publish Shelly.Cli/Shelly.Cli.csproj -c Release -o publish/shelly-cli -p:InstructionSet=x86-64
          cd publish/shelly-cli
          rm -f *.dbg *.pdb
          # generate completion scripts from the freshly built binary
          ./shelly completions fish > shelly.fish
          ./shelly completions zsh  > _shelly
          tar -czf ../Shelly-CLI-linux-x64.tar.gz .
          cd ../..

      - name: Publish Shelly.Gtk
        run: |
          dotnet publish Shelly.Gtk/Shelly.Gtk.csproj -c Release -o publish/shelly-ui -p:InstructionSet=x86-64
          cp Shelly.Gtk/Assets/shellylogo.png publish/shelly-ui/
          cp Shelly.Gtk/Assets/shellylogo-tray.png publish/shelly-ui/
          cp Shelly.Gtk/Assets/shellylogo-update.png publish/shelly-ui/
          # Also bundle the CLI binary for convenience
          cp publish/shelly-cli/shelly publish/shelly-ui/

      - name: Compile and bundle translations
        run: |
          mkdir -p publish/shelly-ui/locale
          for po_file in Shelly.Gtk/po/*.po; do
            if [ -f "$po_file" ]; then
              lang=$(basename "$po_file" .po)
              mkdir -p "publish/shelly-ui/locale/$lang/LC_MESSAGES"
              msgfmt "$po_file" -o "publish/shelly-ui/locale/$lang/LC_MESSAGES/shelly-ui.mo"
            fi
          done

      - name: Compile and bundle tray service translations
        run: |
          mkdir -p publish/shelly-ui/locale
          for po_file in Shelly.Notifications/po/*.po; do
            if [ -f "$po_file" ]; then
              lang=$(basename "$po_file" .po)
              mkdir -p "publish/shelly-ui/locale/$lang/LC_MESSAGES"
              msgfmt "$po_file" -o "publish/shelly-ui/locale/$lang/LC_MESSAGES/shelly-notifications.mo"
            fi
          done

      - name: Publish Shelly.Notifications
        run: |
          meson setup build-notify Shelly.Notifications
          meson compile -C build-notify
          # Bundle Notifications into the UI directory
          cp build-notify/shelly-notifications publish/shelly-ui/

      - name: Publish Shelly.Keys
        run: |
          dotnet publish Shelly.Keys/Shelly.Keys.csproj -c Release -o publish/shelly-keys -p:InstructionSet=x86-64
          # Bundle Keys into the UI directory so it ends up in Shelly-ALPM-linux-x64.tar.gz
          cp publish/shelly-keys/shelly-keys publish/shelly-ui/

      - name: Package Shelly.Keys standalone
        run: |
          cd publish/shelly-keys
          rm -f *.dbg *.pdb
          tar -czf ../Shelly-Keys-linux-x64.tar.gz .
          cd ../..

      - name: Bundle Shelly-ALPM
        run: |
          cp publish/shelly-cli/shelly.fish publish/shelly-ui/
          cp publish/shelly-cli/_shelly     publish/shelly-ui/
          cd publish/shelly-ui
          rm -f *.dbg *.pdb
          tar -czf ../Shelly-ALPM-linux-x64.tar.gz .
          cd ../..

      - name: Copy standalone completions
        run: |
          cp publish/shelly-cli/shelly.fish publish/shelly.fish
          cp publish/shelly-cli/_shelly     publish/_shelly

      - name: Generate SHA256 checksums
        run: |
          cd publish
          # Download the source tarball from GitHub and generate its checksum
          VERSION=${GITHUB_REF#refs/tags/}
          curl -sL "https://github.com/ZoeyErinBauer/Shelly-ALPM/archive/${VERSION}.tar.gz" -o "source-${VERSION}.tar.gz"
          sha256sum "source-${VERSION}.tar.gz" | sed "s/source-${VERSION}.tar.gz/${VERSION}.tar.gz/" > SHA256SUMS.txt
          # Add binary tarballs and completions checksums
          sha256sum Shelly-ALPM-linux-x64.tar.gz Shelly-CLI-linux-x64.tar.gz Shelly-Keys-linux-x64.tar.gz shelly.fish _shelly >> SHA256SUMS.txt
          rm "source-${VERSION}.tar.gz"
          cat SHA256SUMS.txt

      - name: Create Release
        uses: softprops/action-gh-release@v3
        with:
          files: |
            publish/Shelly-ALPM-linux-x64.tar.gz
            publish/Shelly-CLI-linux-x64.tar.gz
            publish/Shelly-Keys-linux-x64.tar.gz
            publish/shelly.fish
            publish/_shelly
            publish/SHA256SUMS.txt
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Deploy Seafoam Labs website
        uses: digitalocean/app_action/deploy@v2
        with:
          app_name: seafoam-labs-website
          token: ${{ secrets.DIGITALOCEAN_ACCESS_TOKEN }}
